# 墨笺网页捕获扩展

1. 在 Chrome 的扩展管理页启用“开发者模式”，选择“加载已解压的扩展程序”；
2. 选择本目录；
3. 在墨笺“设置 → 浏览器捕获”创建令牌，并粘贴到扩展弹窗；
4. 点击扩展图标或按 `⌘⇧K` 捕获当前页面。

扩展会保存 URL、标题、当前选区和可见区域截图。墨笺 API 暂时不可用时，捕获会
留在 `chrome.storage.local` 队列中，并在浏览器启动和每分钟定时重试。
